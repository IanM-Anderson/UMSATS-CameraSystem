/*
read all the 10-DOF sensor values and return an array of their values
*/

float read10DOFData(float arr){ // not sure if this param is right, it is supposed to be an array.
    // the function is passed an array
    // read the values from the sensors
    // then return the array with all the values from the sensors

    return arr;
}